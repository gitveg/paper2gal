## chunk的提前生成

减少用户等待，chunk2的生成可以在用户与chunk1交互的时候就生成好，然后放在缓冲区。

## role的拓展

为了不局限于奈奈这一个角色，可以创建一个角色的设定类RoleConfig，可以通过设计Prompt创建多个角色，使得交互语句的风格不一样。

## 当前Agent工具调用能力

gal人物在讲解的时候需要结合论文的图片，这个时候需要Agent自动调用。